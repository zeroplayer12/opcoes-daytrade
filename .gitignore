# Python
__pycache__/
*.py[cod]
*.egg-info/

# Ambientes virtuais
.venv/
venv/
env/

# Segredos do Streamlit (NUNCA commitar)
.streamlit/secrets.toml
.env
# configuração dos avisos (token do Telegram) — o padrão é gravar fora da pasta, em %LOCALAPPDATA%
avisos.json

# Grades exportadas do opcoes.net.br — dados de mercado, não vão pro repo
*.csv
*.xlsx
*.xls
*.xlsm

# Cotações gravadas pelo coletor RTD do Profit (ficam só no PC)
dados_rt/

# Logs e temporários
*.log
.DS_Store
Thumbs.db

# Binario do tunel (54 MB, baixado da Cloudflare)
cloudflared.exe

# copia gerada para o deploy (build sincroniza a partir de app.py)
web/static/app.py
